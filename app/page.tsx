"use client";

import React from "react";

export default function Page() {
  return React.createElement(
    "div",
    { style: { padding: 20, fontFamily: "system-ui" } },
    "OK — Page fonctionne"
  );
}
